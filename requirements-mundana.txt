# Abu Oracle Mundana Publisher — dependencias del Cloud Run Job
# Instalar con: pip install -r requirements-mundana.txt

# Cálculo astrológico
pyswisseph>=2.10.3.2

# Claude Sonnet 4.6 via Vertex AI (ADC — usa créditos GCP, sin ANTHROPIC_API_KEY)
anthropic[vertex]>=0.49.0

# HTTP (publishers Farcaster, Bluesky, Resend, Neynar)
requests>=2.32.0

# Reddit
praw>=7.8.0

# GCS (registro de predicciones — opcional)
google-cloud-storage>=2.19.0

# Generación de imágenes
matplotlib>=3.8.0
Pillow>=10.0.0
numpy>=1.26.0

# RAG doctrinal — extracción PDF + índice BM25
pymupdf>=1.24.0
rank-bm25>=0.2.2

# Utilidades estándar (incluidas en Python 3.11)
# json, hashlib, datetime, pathlib, os, sys — sin instalar
